---
title: "Treelikeness"
params:
  # general
  rmddir: ""
  prefix: ""
  outdir: ""
  redo: FALSE
  
  # software directories
  treelikenessdir: ""
  iqtree2dir: ""
  splittreedir: ""
---

```{r setup, include=FALSE}
library(log4r)

source(paste(params$treelikenessdir, "/code/func_test_statistic.R", sep=""))

currentdir <- paste(params$outdir, "/", params$prefix, sep="")
if (!file.exists(currentdir)) {
  dir.create(currentdir, recursive = T)
}

fn_log <- paste(params$outdir, "/", params$prefix, "/", params$prefix, ".tl.log", sep="")
log_appender <- file_appender(fn_log, append = TRUE, layout = default_log_layout())
fn_logger <- log4r::logger(threshold = "INFO", appenders = log_appender)
if (params$redo == TRUE) {
  unlink(fn_log)
}

if (!file.exists(fn_log)) {
  write.table("SimNOW", file=fn_log, quote=F, row.names=F, col.names=F)
}

knitr::opts_knit$set(root.dir = currentdir)
```

## Treelikeness Analysis
```{r child = paste(params$rmddir,"/tl_run.Rmd",sep=""), include=FALSE}
```