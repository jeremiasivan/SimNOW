---
title: "MAST and HMM"
params:
  # general
  rmddir: "~/Documents/SimNOW/rmd"
  prefix: "wrap_6"
  outdir: "~/Documents/simulation/rmd_test"
  redo: TRUE
  
  # sequence simulation
  iqtree2dir: "~/Downloads/iqtree-2.2.2.2-MacOSX/bin/iqtree2"
  
  # mast and hmm
  mast_model: "JC+FO+T"
  mast_tops: 1
---

```{r setup, include=FALSE}
library(dplyr)
library(data.table)
library(ggplot2)
library(forcats)

currentdir <- paste(params$outdir, "/", params$prefix, sep="")
if (!file.exists(currentdir)) {
  dir.create(currentdir, recursive = T)
}

knitr::opts_knit$set(include = FALSE, root.dir = currentdir)
```

## MASTHMM Analysis
```{r child = paste(params$rmddir,"/masthmm.Rmd",sep="")}
```

## MASTHMM Summary
```{r child = paste(params$rmddir,"/masthmm_summary.Rmd",sep="")}
```